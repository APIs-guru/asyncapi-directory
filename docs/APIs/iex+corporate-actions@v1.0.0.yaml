x-collection-name: IEX
info:
  title: IEX Corporate Actions (stream)
  version: 1.0.0
  description: This is a streaming API that has been autogenerated from the IEX
    using Streamdata.io.
servers:
  - url: api.iextrading.com/ref-data/daily-list/symbol-directory
    scheme: https
    variables:
      format:
        description: " Parameter is optional Value can be csv or psv When parameter is
          not present, format defaults to JSON"
      token:
        description: " Parameter is optional Value is the API token from your IEX user
          account If you have been permissioned for CUSIP information
          you&rsquo;ll receive a CUSIP field, othewise data defaults to exclude
          CUSIP."
stream:
  framing:
    type: chunked
    delimiter: \r\n
  read:
    - $ref: "#/components/messages/ref-data_daily-list_symbol-directory"
asyncapi: 1.2.0
components:
  messages:
    ref-data_daily-list_symbol-directory: {}
